var store = require('../store');

var {{objectName}} = Ember.ObjectController.extend({

  destroy: function() {
    if (!confirm('Are you sure?')) return;
    this.get('model').deleteRecord();
    store.commit();
    this.get('target').transitionTo('{{resourcesRoute}}');
  }

});

module.exports = {{objectName}};

